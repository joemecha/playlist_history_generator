require 'csv'

class ExportToCsvService
  # def initialize(playlists:)
  #   @playlists = playlists
  # end

  # def call
  #   CSV.generate(headers: true) do |csv|
  #     csv << ['Playlist Name', 'Track Name', 'Artist Name']

  #     @playlists.each do |playlist|
  #       playlist.songs.each do |song|
  #         csv << [playlist.name, song.name, song.artist]
  #       end
  #     end
  #   end
  # end
end
